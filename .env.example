# =================================
# 프로덕션 환경 변수 예시
# =================================
# 이 파일을 .env로 복사하고 실제 값으로 변경하세요
# cp .env.example .env

# MySQL 설정
MYSQL_ROOT_PASSWORD=your_secure_root_password_here
MYSQL_DATABASE=signly_prod
MYSQL_USER=signly_user
MYSQL_PASSWORD=your_secure_mysql_password_here

# Redis 설정
REDIS_PASSWORD=your_secure_redis_password_here

# JWT 설정 (최소 32자 이상의 랜덤 문자열)
JWT_SECRET=your_jwt_secret_key_minimum_32_characters_long_random_string

# Docker 이미지 (CD 배포 시 사용)
APP_IMAGE=signly-app:latest

# 애플리케이션 포트
APP_PORT=8080

# 파일 업로드 설정
MAX_FILE_SIZE=10MB
MAX_REQUEST_SIZE=10MB

# 이메일 설정
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-specific-password
EMAIL_FROM=noreply@yourdomain.com

# OAuth2 Google 설정
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Base URL (OAuth2 redirect URI용)
APP_BASE_URL=https://yourdomain.com

# CORS 설정 (쉼표로 구분)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# 로그 레벨
LOG_LEVEL=INFO
SECURITY_LOG_LEVEL=WARN
WEB_LOG_LEVEL=WARN
HIBERNATE_SQL_LEVEL=WARN

# JVM 옵션
JAVA_OPTS=-Xms1024m -Xmx2048m -XX:+UseG1GC -XX:MaxGCPauseMillis=200

# =================================
# GitHub Secrets (GitHub Actions에 등록 필요)
# =================================
# Repository Settings > Secrets and variables > Actions에서 다음 시크릿 등록:
#
# DOCKER_USERNAME=your-dockerhub-username
# DOCKER_PASSWORD=your-dockerhub-password-or-token
#
# DEPLOY_HOST=your-server-ip-or-domain
# DEPLOY_USER=your-ssh-username
# DEPLOY_SSH_KEY=your-ssh-private-key
# DEPLOY_PORT=22
# DEPLOY_PATH=/opt/deally
